import { Component } from '@angular/core';
import { SearchBar } from '../../components';

@Component({
  selector: 'app-home',
  imports: [SearchBar],
  templateUrl: './home.html',
})
export class Home {

}
